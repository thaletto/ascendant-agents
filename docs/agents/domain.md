# Domain Docs

Before domain or architecture work, read root `CONTEXT.md` and relevant files
under `docs/adr/` when they exist. Their absence is not an error.

This is a single-context repository:

```
/
├── CONTEXT.md
├── docs/adr/
└── plugins/
```

Use glossary terms from `CONTEXT.md` in issues, proposals, hypotheses, and
tests. Surface any conflict with an existing ADR.
